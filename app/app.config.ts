export default defineAppConfig({
  ui: {
    colors: {
      primary: 'sky',
      neutral: 'slate'
    }
  },
  lingaphoneUi: {
    dashboard: {
      heroRadius: '1.25rem',
      heroPadding: 'clamp(1.25rem, 2vw, 2rem)',
      heroShadow: 'var(--shadow-warm-lg)'
    }
  }
})
