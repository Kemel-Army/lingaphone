/**
 * Backward-compat auto-import stub.
 * Logic lives in entities/progress — import directly in new pages:
 *   import { useStudentProgress } from '~/entities/progress'
 */
export { useStudentProgress } from '~/entities/progress'
export type {
  TopicMastery,
  ActivityCell,
  PronunciationGap,
  VocabularyBuckets,
  StoryStats
} from '~/entities/progress'
