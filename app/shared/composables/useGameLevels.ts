/**
 * Backward-compat auto-import stub.
 * Logic lives in entities/game-profile — import directly in new pages:
 *   import { useGameLevels } from '~/entities/game-profile'
 */
export { useGameLevels } from '~/entities/game-profile'
export type { LevelProgress } from '~/entities/game-profile'
